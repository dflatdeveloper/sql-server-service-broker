USE MASTER

CREATE ENDPOINT ENDPOINT_MACHINE
STATE = STOPPED
AS TCP (
	LISTENER_PORT = 4022
)
FOR SERVICE_BROKER
	(AUTHENTICATION = CERTIFICATE TRANSPORT_CERT_MACHINE, ENCRYPTION = SUPPORTED)
GO

CREATE LOGIN MACHINE_LOGIN
	FROM CERTIFICATE TRANSPORT_CERT_MACHINE
GO